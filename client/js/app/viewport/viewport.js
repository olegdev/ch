/**
 * Модуль вьюпорта. Отвечает за первоначальное создание интерфейса, используется для доступа к отрисованным вью
 *
 */
define([
	'jquery',
	'underscore',
	'backbone',
	'marionette'
], function($, _, Backbone, Mn) {

	return {
		render: function(user) {

		}
	};
});